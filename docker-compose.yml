version: "3.8"

services:
#  ชื่อเดียวกับ container name ห้ามซ้ำกับ container อื่นที่มี
  flutter-web-prince2:
    build: .
    container_name: flutter-web-prince2
    ports:
      - "8085:80"
    networks:
      - docker-network

networks:
  docker-network:
    external: true